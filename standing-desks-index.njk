---
pagination:
  data: standing_desks.desks
  size: 1000
  alias: desks_page
permalink: "standing-desks/index.html"
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Best Standing Desks — Filter by Height, Budget & Features | Ergely</title>
  <meta name="description" content="Find the right standing desk for your height. Ergely Fit Score™ calculates compatibility based on your body. Verified specs, no sponsored rankings.">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="https://ergely.com/standing-desks/">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root { --black:#1B1B1B;--accent:#E05A2B;--white:#FFFFFF;--offwhite:#F7F7F5;--border:#E4E4E0;--muted:#6B6B6B;--font:'Inter',system-ui,sans-serif; }
    body { font-family:var(--font);background:var(--white);color:var(--black);font-size:15px;line-height:1.6; }
    a { color:inherit;text-decoration:none; }
    nav { background:#fff;height:56px;display:flex;align-items:center;padding:0 32px;position:sticky;top:0;z-index:200;gap:32px;border-bottom:1px solid var(--border);box-shadow:0 1px 4px rgba(0,0,0,0.06); }
    .logo { font-size:19px;font-weight:800;letter-spacing:-0.03em;color:var(--black);flex-shrink:0; }
    .logo span { color:var(--accent); }
    .nav-links { display:flex;gap:2px;flex:1; }
    .nav-links a { color:var(--black);font-size:13px;font-weight:500;padding:5px 12px;border-radius:5px;transition:color 0.12s,background 0.12s;white-space:nowrap; }
    .nav-links a:hover { color:var(--accent);background:#fff5f2; }
    .nav-links a.active { color:var(--accent);font-weight:600; }
    .nav-right { margin-left:auto;display:flex;gap:8px; }
    .nav-right a { color:var(--muted);font-size:13px;font-weight:500;padding:5px 12px;border-radius:5px; }
    .nav-right a:hover { color:var(--black); }
    .hamburger { display:none;flex-direction:column;gap:5px;cursor:pointer;padding:4px;margin-left:auto;background:none;border:none; }
    .hamburger span { display:block;width:22px;height:2px;background:var(--black);border-radius:2px; }
    .mobile-menu { display:none;position:fixed;top:56px;left:0;right:0;background:#fff;border-bottom:1px solid var(--border);padding:12px 0;z-index:199;box-shadow:0 4px 16px rgba(0,0,0,0.08); }
    .mobile-menu.open { display:block; }
    .mobile-menu a { display:block;padding:12px 24px;font-size:15px;font-weight:500;color:var(--black);border-bottom:1px solid var(--offwhite); }
    .mobile-menu a.active { color:var(--accent);font-weight:600; }
    .page-header { background:var(--offwhite);border-bottom:1px solid var(--border);padding:28px 32px; }
    .page-header-inner { max-width:1100px;margin:0 auto; }
    .page-header h1 { font-size:22px;font-weight:800;letter-spacing:-0.02em;margin-bottom:4px; }
    .page-header p { font-size:13px;color:var(--muted); }
    .listing-wrap { max-width:1100px;margin:0 auto;padding:28px 32px 64px;display:grid;grid-template-columns:260px 1fr;gap:32px;align-items:start; }
    .sidebar { position:sticky;top:76px;display:flex;flex-direction:column;gap:6px; }
    .fit-finder { background:#fff8f5;border:1.5px solid #f0c4b0;border-radius:10px;padding:16px;margin-bottom:8px; }
    .fit-finder-title { font-size:13px;font-weight:700;color:var(--black);margin-bottom:12px; }
    .fit-finder-inputs { display:flex;flex-direction:column;gap:8px;margin-bottom:12px; }
    .fit-finder-field { display:flex;flex-direction:column;gap:3px; }
    .fit-finder-field label { font-size:11px;font-weight:600;color:var(--muted);text-transform:uppercase;letter-spacing:0.06em; }
    .fit-finder-field input { padding:8px 10px;border:1px solid var(--border);border-radius:6px;font-size:14px;font-family:var(--font);background:#fff;width:100%; }
    .fit-finder-field input:focus { outline:2px solid var(--accent);border-color:transparent; }
    .btn-find { width:100%;padding:10px;background:var(--accent);color:#fff;font-weight:700;font-size:13px;border:none;border-radius:6px;cursor:pointer;font-family:var(--font); }
    .btn-find:hover { background:#c44e24; }
    .fit-finder-result { display:none;margin-top:10px;font-size:12px;color:var(--muted); }
    .fit-finder-result.active { display:block; }
    .filter-section { border:1px solid var(--border);border-radius:10px;overflow:hidden; }
    .filter-header { padding:12px 14px;background:var(--offwhite);font-size:12px;font-weight:700;color:var(--black);text-transform:uppercase;letter-spacing:0.07em;display:flex;justify-content:space-between;align-items:center;cursor:pointer;border:none;width:100%;font-family:var(--font); }
    .filter-header:hover { background:#ececea; }
    .filter-arrow { font-size:10px;color:var(--muted);transition:transform 0.2s; }
    .filter-header[aria-expanded="false"] .filter-arrow { transform:rotate(-90deg); }
    .filter-body { padding:12px 14px;background:#fff; }
    .filter-body.hidden { display:none; }
    .pill-group { display:flex;flex-wrap:wrap;gap:6px; }
    .pill { font-size:12px;font-weight:500;padding:5px 11px;border-radius:99px;border:1px solid var(--border);background:#fff;color:var(--black);cursor:pointer;font-family:var(--font); }
    .pill:hover { border-color:var(--accent);color:var(--accent); }
    .pill.active { background:var(--accent);border-color:var(--accent);color:#fff; }
    .toggle-group { display:flex;flex-direction:column;gap:8px; }
    .toggle-item { display:flex;align-items:center;justify-content:space-between;font-size:13px; }
    .toggle-switch { position:relative;width:36px;height:20px; }
    .toggle-switch input { opacity:0;width:0;height:0; }
    .toggle-track { position:absolute;inset:0;background:var(--border);border-radius:99px;transition:background 0.2s;cursor:pointer; }
    .toggle-switch input:checked + .toggle-track { background:var(--accent); }
    .toggle-track::after { content:'';position:absolute;width:14px;height:14px;background:#fff;border-radius:50%;top:3px;left:3px;transition:transform 0.2s; }
    .toggle-switch input:checked + .toggle-track::after { transform:translateX(16px); }
    .reset-btn { font-size:12px;color:var(--muted);background:none;border:none;cursor:pointer;font-family:var(--font);padding:4px 0;text-decoration:underline; }
    .reset-btn:hover { color:var(--accent); }
    .results-area { min-width:0; }
    .results-bar { display:flex;align-items:center;justify-content:space-between;margin-bottom:20px; }
    .results-count { font-size:13px;color:var(--muted); }
    .results-count strong { color:var(--black); }
    .sort-select { font-size:13px;font-family:var(--font);padding:6px 10px;border:1px solid var(--border);border-radius:6px;background:#fff;cursor:pointer; }
    .fit-score-header { display:none;margin-bottom:16px;padding:12px 16px;background:var(--offwhite);border-radius:8px;border:1px solid var(--border); }
    .fit-score-header.visible { display:block; }
    .fit-score-header-title { font-size:13px;font-weight:700;color:var(--black); }
    .fit-score-header-sub { font-size:12px;color:var(--muted);margin-top:2px; }
    .product-grid { display:grid;grid-template-columns:repeat(3,1fr);gap:16px; }
    .product-card { border:1px solid var(--border);border-radius:10px;overflow:hidden;background:#fff;transition:border-color 0.15s,box-shadow 0.15s,transform 0.12s;display:flex;flex-direction:column; }
    .product-card:hover { border-color:var(--accent);box-shadow:0 4px 16px rgba(224,90,43,0.1);transform:translateY(-2px); }
    .product-card.hidden { display:none; }
    .card-image { aspect-ratio:4/3;background:#fff;display:flex;align-items:center;justify-content:center;overflow:hidden;position:relative;border-bottom:1px solid var(--border); }
    .card-image img { width:100%;height:100%;object-fit:cover; }
    .card-image .no-image { font-size:32px;color:var(--muted); }
    .fit-badge { position:absolute;bottom:8px;right:8px;display:none;flex-direction:column;align-items:center;border-radius:8px;padding:5px 8px;min-width:64px;text-align:center;box-shadow:0 2px 6px rgba(0,0,0,0.12); }
    .fit-badge.visible { display:flex; }
    .fit-badge-score { font-size:14px;font-weight:800;line-height:1;letter-spacing:-0.02em; }
    .fit-badge-match { font-size:9px;font-weight:600;margin-top:2px;line-height:1.2; }
    .fit-badge.excellent { background:#EAF4EE;color:#1E6B3A;border:1px solid #A8D5B5; }
    .fit-badge.good-match { background:#E8F4FF;color:#1A5FA8;border:1px solid #A8CBF0; }
    .fit-badge.fair { background:#FFF8E7;color:#8A6000;border:1px solid #E0C96A; }
    .fit-badge.limited { background:#FAEAEA;color:#9B2020;border:1px solid #E8A8A8; }
    .card-body { padding:14px;flex:1;display:flex;flex-direction:column;gap:6px; }
    .card-brand { font-size:10px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:0.08em; }
    .card-model { font-size:14px;font-weight:700;color:var(--black);line-height:1.3; }
    .card-summary { font-size:12px;color:var(--muted);line-height:1.45;flex:1; }
    .card-specs { display:flex;gap:6px;flex-wrap:wrap;margin-top:4px; }
    .card-spec { font-size:11px;color:var(--muted);background:var(--offwhite);padding:2px 7px;border-radius:4px; }
    .card-footer { padding:12px 14px;border-top:1px solid var(--border);display:flex;align-items:center;justify-content:space-between; }
    .card-price { font-size:15px;font-weight:800;letter-spacing:-0.02em; }
    .card-cta { font-size:12px;font-weight:600;color:var(--accent); }
    .card-cta:hover { text-decoration:underline; }
    .no-results { display:none;text-align:center;padding:48px 24px;color:var(--muted); }
    .no-results.visible { display:block; }
    .no-results h3 { font-size:16px;font-weight:700;color:var(--black);margin-bottom:8px; }
    footer { background:#111;padding:48px 32px 0; }
    .footer-grid { max-width:1100px;margin:0 auto;display:grid;grid-template-columns:2fr 1fr 1fr 1fr;gap:48px;padding-bottom:48px;border-bottom:1px solid #2a2a2a; }
    .footer-logo { font-size:18px;font-weight:800;color:#fff;margin-bottom:12px;display:block; }
    .footer-logo span { color:var(--accent); }
    .footer-tagline { font-size:13px;color:#999;line-height:1.6;margin-bottom:16px; }
    .footer-copy { font-size:12px;color:#666; }
    .footer-col h4 { font-size:11px;font-weight:700;letter-spacing:0.08em;text-transform:uppercase;color:#aaa;margin-bottom:16px; }
    .footer-col a { display:block;font-size:13px;color:#999;margin-bottom:9px;transition:color 0.12s; }
    .footer-col a:hover { color:#fff; }
    .footer-bottom { max-width:1100px;margin:0 auto;padding:18px 0;display:flex;justify-content:space-between;font-size:12px;color:#666;gap:24px; }
    @media(max-width:900px){.listing-wrap{grid-template-columns:1fr;gap:20px;padding:20px 20px 48px;}.sidebar{position:static;}.product-grid{grid-template-columns:repeat(2,1fr);}.footer-grid{grid-template-columns:1fr 1fr;gap:32px;}}
    @media(max-width:600px){nav{padding:0 16px;}.nav-links,.nav-right{display:none;}.hamburger{display:flex;}.product-grid{grid-template-columns:1fr;}.footer-grid{grid-template-columns:1fr;gap:28px;}.footer-bottom{flex-direction:column;text-align:center;}}
  </style>
</head>
<body>
<nav>
  <a href="/" class="logo">Erge<span>ly</span></a>
  <div class="nav-links">
    <a href="/standing-desks/" class="active">Standing Desks</a>
  </div>
  <div class="nav-right">
    <a href="/guides/">Guides</a>
    <a href="/tools/desk-size-finder/">Size Finder</a>
  </div>
  <button class="hamburger" onclick="toggleMenu()" aria-label="Menu"><span></span><span></span><span></span></button>
</nav>
<div class="mobile-menu" id="mobile-menu">
  <a href="/standing-desks/" class="active">Standing Desks</a>
</div>

<div class="page-header">
  <div class="page-header-inner">
    <h1>🖥️ Standing Desks</h1>
    <p>{{ desks_page | length }} desks with verified specs — find the one that fits your height</p>
  </div>
</div>

<div class="listing-wrap">
  <aside class="sidebar">
    <div class="fit-finder">
      <div class="fit-finder-title">Find the right desk for you</div>
      <div class="fit-finder-inputs">
        <div class="fit-finder-field">
          <label for="ff-height">Your height (cm)</label>
          <input type="number" id="ff-height" placeholder="e.g. 175" min="140" max="220">
        </div>
      </div>
      <button class="btn-find" onclick="applyFitFinder()">Find desks →</button>
      <div class="fit-finder-result" id="ff-result"></div>
    </div>

    <div class="filter-section">
      <button class="filter-header" aria-expanded="true" onclick="toggleFilter(this)">Budget <span class="filter-arrow">▾</span></button>
      <div class="filter-body">
        <div class="pill-group">
          <button class="pill" data-value="under_500" onclick="togglePill(this,'budget')">Under $500</button>
          <button class="pill" data-value="500_1000" onclick="togglePill(this,'budget')">$500–1000</button>
          <button class="pill" data-value="1000_plus" onclick="togglePill(this,'budget')">$1000+</button>
        </div>
      </div>
    </div>

    <div class="filter-section">
      <button class="filter-header" aria-expanded="true" onclick="toggleFilter(this)">Brand <span class="filter-arrow">▾</span></button>
      <div class="filter-body">
        <div class="pill-group">
          <button class="pill" data-value="FlexiSpot" onclick="togglePill(this,'brand')">FlexiSpot</button>
          <button class="pill" data-value="Uplift" onclick="togglePill(this,'brand')">Uplift</button>
          <button class="pill" data-value="Autonomous" onclick="togglePill(this,'brand')">Autonomous</button>
          <button class="pill" data-value="Fully" onclick="togglePill(this,'brand')">Fully</button>
          <button class="pill" data-value="IKEA" onclick="togglePill(this,'brand')">IKEA</button>
          <button class="pill" data-value="Ergonofis" onclick="togglePill(this,'brand')">Ergonofis</button>
        </div>
      </div>
    </div>

    <div class="filter-section">
      <button class="filter-header" aria-expanded="true" onclick="toggleFilter(this)">Motors <span class="filter-arrow">▾</span></button>
      <div class="filter-body">
        <div class="pill-group">
          <button class="pill" data-value="1" onclick="togglePill(this,'motors')">Single motor</button>
          <button class="pill" data-value="2" onclick="togglePill(this,'motors')">Dual motor</button>
        </div>
      </div>
    </div>

    <div class="filter-section">
      <button class="filter-header" aria-expanded="true" onclick="toggleFilter(this)">Features <span class="filter-arrow">▾</span></button>
      <div class="filter-body">
        <div class="toggle-group">
          <div class="toggle-item">
            <span>Memory presets</span>
            <label class="toggle-switch"><input type="checkbox" id="tog-presets" onchange="applyFilters()"><span class="toggle-track"></span></label>
          </div>
          <div class="toggle-item">
            <span>Anti-collision</span>
            <label class="toggle-switch"><input type="checkbox" id="tog-collision" onchange="applyFilters()"><span class="toggle-track"></span></label>
          </div>
        </div>
      </div>
    </div>

    <button class="reset-btn" onclick="resetAll()">Reset all filters</button>
  </aside>

  <div class="results-area">
    <div class="results-bar">
      <div class="results-count"><strong id="count">{{ desks_page | length }}</strong> desks found</div>
      <select class="sort-select" onchange="applySort(this.value)">
        <option value="">Sort: Relevance</option>
        <option value="price_asc">Price: Low → High</option>
        <option value="price_desc">Price: High → Low</option>
      </select>
    </div>

    <div class="fit-score-header" id="fit-score-header">
      <div class="fit-score-header-title">Your Ergely Fit Scores™</div>
      <div class="fit-score-header-sub" id="fit-score-sub"></div>
    </div>

    <div class="product-grid" id="product-grid">
      {% for desk in desks_page %}
      <a class="product-card"
         href="/standing-desks/{{ desk.id }}/"
         data-id="{{ desk.id }}"
         data-brand="{{ desk.identity.brand }}"
         data-budget="{{ desk.identity.price_range }}"
         data-price="{{ desk.identity.price_usd if desk.identity.price_usd else 0 }}"
         data-motors="{{ desk.specs.motors if desk.specs.motors else '' }}"
         data-presets="{{ 'true' if desk.specs.memory_presets and desk.specs.memory_presets > 0 else 'false' }}"
         data-collision="{{ 'true' if desk.specs.anti_collision else 'false' }}"
         data-height-min="{{ desk.specs.height_min_cm if desk.specs.height_min_cm else '' }}"
         data-height-max="{{ desk.specs.height_max_cm if desk.specs.height_max_cm else '' }}">
        <div class="card-image">
          {% if desk.identity.image_url %}
            <img src="{{ desk.identity.image_url }}" alt="{{ desk.identity.brand }} {{ desk.identity.model }}" loading="lazy">
          {% else %}
            <div class="no-image">🖥️</div>
          {% endif %}
          <span class="fit-badge" id="badge-{{ desk.id }}"></span>
        </div>
        <div class="card-body">
          <div class="card-brand">{{ desk.identity.brand }}</div>
          <div class="card-model">{{ desk.identity.model }}</div>
          <div class="card-summary">{{ desk.editorial.best_for_summary }}</div>
          <div class="card-specs">
            {% if desk.specs.height_min_cm and desk.specs.height_max_cm %}
            <span class="card-spec">{{ desk.specs.height_min_cm }}–{{ desk.specs.height_max_cm }} cm</span>
            {% endif %}
            {% if desk.specs.motors %}
            <span class="card-spec">{{ desk.specs.motors }} motor{% if desk.specs.motors > 1 %}s{% endif %}</span>
            {% endif %}
            {% if desk.specs.memory_presets %}
            <span class="card-spec">{{ desk.specs.memory_presets }} presets</span>
            {% endif %}
            {% if desk.specs.warranty_years %}
            <span class="card-spec">{{ desk.specs.warranty_years }}yr warranty</span>
            {% endif %}
          </div>
        </div>
        <div class="card-footer">
          <div class="card-price">{% if desk.identity.price_usd %}${{ desk.identity.price_usd }}{% else %}—{% endif %}</div>
          <span class="card-cta">View desk →</span>
        </div>
      </a>
      {% endfor %}
    </div>
    <div class="no-results" id="no-results"><h3>No desks match your filters</h3><p>Try removing some filters.</p></div>
  </div>
</div>

<footer>
  <div class="footer-grid">
    <div><span class="footer-logo">Erge<span>ly</span></span><p class="footer-tagline">The ergonomic compatibility platform. Real specs, verified data, no sponsored rankings.</p><p class="footer-copy">© 2026 Ergely. Independent.</p></div>
    <div class="footer-col"><h4>Browse</h4><a href="/standing-desks/">Standing Desks</a></div>
    <div class="footer-col"><h4>Tools</h4><a href="/tools/desk-size-finder/">Desk Size Finder</a><a href="/guides/">Guides</a></div>
    <div class="footer-col"><h4>About</h4><a href="/about/">About</a><a href="/contact/">Contact</a></div>
  </div>
  <div class="footer-bottom"><span>Ergely is reader-supported. We may earn a referral fee at no cost to you.</span><span>Not medical advice.</span></div>
</footer>

<script>
  const state = { budget:null, brand:null, motors:null, presets:false, collision:false };
  function toggleMenu() { document.getElementById('mobile-menu').classList.toggle('open'); }
  document.addEventListener('click', function(e) { const m=document.getElementById('mobile-menu'),h=document.querySelector('.hamburger'); if(!m.contains(e.target)&&!h.contains(e.target)) m.classList.remove('open'); });
  function toggleFilter(btn) { const b=btn.nextElementSibling,o=btn.getAttribute('aria-expanded')==='true'; btn.setAttribute('aria-expanded',String(!o)); b.classList.toggle('hidden',o); }
  function togglePill(btn,type) {
    const g=btn.closest('.pill-group'),w=btn.classList.contains('active');
    g.querySelectorAll('.pill').forEach(p=>p.classList.remove('active'));
    if(!w){btn.classList.add('active');state[type]=btn.dataset.value;}else{state[type]=null;}
    applyFilters();
  }
  function calcFitScore(h, card) {
    const hMin=parseFloat(card.dataset.heightMin);
    const hMax=parseFloat(card.dataset.heightMax);
    if(!hMin||!hMax) return null;
    const idealSit=h*0.43;
    const idealStand=h*0.60;
    let sitScore=100;
    if(idealSit<hMin){const d=hMin-idealSit;sitScore=d<=2?90:d<=5?70:d<=10?45:10;}
    else if(idealSit>hMax){const d=idealSit-hMax;sitScore=d<=2?90:d<=5?70:d<=10?45:10;}
    let standScore=100;
    if(idealStand<hMin){const d=hMin-idealStand;standScore=d<=2?90:d<=5?70:d<=10?45:10;}
    else if(idealStand>hMax){const d=idealStand-hMax;standScore=d<=2?90:d<=5?70:d<=10?45:10;}
    return Math.round(sitScore*0.60+standScore*0.40);
  }
  function scoreToClass(s){if(s>=90)return'excellent';if(s>=75)return'good-match';if(s>=60)return'fair';return'limited';}
  function scoreToLabel(s){if(s>=90)return'Excellent Match';if(s>=75)return'Good Match';if(s>=60)return'Fair Match';return'Limited Match';}
  function applyFitFinder() {
    const h=parseFloat(document.getElementById('ff-height').value);
    if(!h) return;
    const cards=document.querySelectorAll('.product-card');
    let excellent=0,good=0,fair=0,limited=0;
    cards.forEach(card=>{
      const badge=document.getElementById('badge-'+card.dataset.id);
      if(!badge) return;
      const score=calcFitScore(h,card);
      if(score===null){badge.className='fit-badge';return;}
      const cls=scoreToClass(score);
      const lbl=scoreToLabel(score);
      badge.className='fit-badge visible '+cls;
      badge.innerHTML='<span class="fit-badge-score">'+score+'/100</span><span class="fit-badge-match">'+lbl+'</span>';
      card.dataset.fitScore=score;
      if(cls==='excellent')excellent++;else if(cls==='good-match')good++;else if(cls==='fair')fair++;else limited++;
      const base=card.getAttribute('href').split('?')[0];
      card.setAttribute('href',base+'?h='+h);
    });
    const result=document.getElementById('ff-result');
    result.textContent=excellent+' Excellent, '+good+' Good, '+fair+' Fair, '+limited+' Limited match.';
    result.classList.add('active');
    const hdr=document.getElementById('fit-score-header');
    const sub=document.getElementById('fit-score-sub');
    if(hdr&&sub){hdr.classList.add('visible');sub.textContent='Matched to your height: '+h+' cm';}
    applyFilters();
  }
  function applyFilters() {
    state.presets=document.getElementById('tog-presets').checked;
    state.collision=document.getElementById('tog-collision').checked;
    const cards=document.querySelectorAll('.product-card');let visible=0;
    cards.forEach(card=>{
      let show=true;
      if(state.brand&&card.dataset.brand!==state.brand)show=false;
      if(state.budget&&card.dataset.budget!==state.budget)show=false;
      if(state.motors&&card.dataset.motors!==state.motors)show=false;
      if(state.presets&&card.dataset.presets!=='true')show=false;
      if(state.collision&&card.dataset.collision!=='true')show=false;
      card.classList.toggle('hidden',!show);if(show)visible++;
    });
    document.getElementById('count').textContent=visible;
    document.getElementById('no-results').classList.toggle('visible',visible===0);
  }
  function applySort(v) {
    const g=document.getElementById('product-grid'),cards=Array.from(g.querySelectorAll('.product-card'));
    cards.sort((a,b)=>v==='price_asc'?parseFloat(a.dataset.price)-parseFloat(b.dataset.price):v==='price_desc'?parseFloat(b.dataset.price)-parseFloat(a.dataset.price):0);
    cards.forEach(c=>g.appendChild(c));
  }
  function resetAll() {
    state.budget=null;state.brand=null;state.motors=null;
    document.getElementById('ff-height').value='';
    document.getElementById('ff-result').classList.remove('active');
    document.getElementById('fit-score-header').classList.remove('visible');
    document.getElementById('tog-presets').checked=false;
    document.getElementById('tog-collision').checked=false;
    document.querySelectorAll('.pill.active').forEach(p=>p.classList.remove('active'));
    document.querySelectorAll('.fit-badge').forEach(b=>{b.className='fit-badge';b.innerHTML='';});
    document.querySelectorAll('.product-card').forEach(c=>{c.classList.remove('hidden');const base=c.getAttribute('href').split('?')[0];c.setAttribute('href',base);});
    document.getElementById('count').textContent=document.querySelectorAll('.product-card').length;
    document.getElementById('no-results').classList.remove('visible');
  }
</script>
</body>
</html>
